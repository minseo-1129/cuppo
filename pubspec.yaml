name: cuppo
version: 2.0.0+3
publish_to: 'none'
description: CUPPO - 개인 커피 기록 앱

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2
  gal: ^2.3.3
  share_plus: ^13.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/ill/
    - assets/sel/
